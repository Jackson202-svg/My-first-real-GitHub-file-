# Thid is my first rela github file 

# ⚙️ How to Run Locally
cd backend
pip install -r requirements.txt
export OPENAI_API_KEY="your-api-key"
python app.py
→ Runs on http://127.0.0.1:5000
#Frontend
cd frontend
npm install
npm start
→ Opens browser at http://127.0.0.1:8080
Now the chatbot is fully working 🎉
# ☁️ How to Put It on GitHub
Create a new repo (e.g. python-chatbot)
Upload the backend and frontend folders
Push to GitHub:
git init
git add .
git commit -m "Initial chatbot commit"
git branch -M main
git remote add origin https://github.com/yourusername/python-chatbot.git
git push -u origin main
